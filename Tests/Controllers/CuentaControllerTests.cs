using System;
using System.Collections.Generic;
//using backendAlquimia.Controllers;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Tests.Controllers
{
    public class CuentaControllerTests
    {
        //[Fact]
        public async Task RegistrarJson_DeberiaRetornarOk_SiElUsuarioEsNuevo()
        {
            // Arrange
            //var userManagerMock = MockHelpers.MockUserManager<Usuario>();
            //userManagerMock.Setup(m => m.FindByEmailAsync(It.IsAny<string>())).ReturnsAsync((Usuario)null);
            //userManagerMock.Setup(m => m.CreateAsync(It.IsAny<Usuario>(), It.IsAny<string>())).ReturnsAsync(IdentityResult.Success);
            //userManagerMock.Setup(m => m.AddToRoleAsync(It.IsAny<Usuario>(), It.IsAny<string>())).ReturnsAsync(IdentityResult.Success);

            //var signInManagerMock = MockHelpers.MockSignInManager<Usuario>();
            //var loggerMock = new Mock<ILogger<CuentaController>>();
            //var jwtMock = new Mock<IJwtService>();

            //var controller = new CuentaController(userManagerMock.Object, signInManagerMock.Object, loggerMock.Object, jwtMock.Object);

            //var dto = new RegisterDTO
            //{
            //    Email = "test@alquimia.com",
            //    Password = "123456",
            //    Name = "Test",
            //    Rol = "Creador"
            //};

            //// Act
            //var result = await controller.RegistrarJson(dto);

            //// Assert
            //Assert.IsType<OkObjectResult>(result);
        }
    }
}
